require('babel-register');
require('./server');

// This separate file and its statements above allow to import modules using ES6 syntax:
// using 'import' instead of 'require', needed for modules in common between client and server
